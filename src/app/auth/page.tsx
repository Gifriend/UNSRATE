import AuthSlider from "@/app/auth/components/AuthSlider"

export const metadata = {
  title: 'Sign In or Sign Up',
  description: 'Sign in to your account or create a new one',
}

export default function AuthPage() {
  return <AuthSlider />
}